const confition = true;
let name;
if (confition === true) {
  name = "트루";
} else {
  name = "팔스";
}

const name2 = confition === true ? "트루" : "팔스";

function example() {
  if (condition1) {
    return value1;
  }
  if (condition2) {
    return value2;
  }
  return value4;
}

const example = 
  return condition1 ? value1
  : condition2 ? value2
  : condition3 ? value3
  : value4;
